# DD2 Second — Executable Function Runtime

## Purpose

Preserve the second worker's observable capability and repeated work character as an executable contract. This runtime is cumulative with the byte-exact embedded `rq-templ` engine. The complete interaction surface is locked separately in `SECOND_TAB_BEHAVIOR_RUNTIME.md`.

For `RUNTIME_PACKAGE_HARDENING`, the certified package and admitted repository evidence are the Source; do not ask for a current chart-value file. A current user-selected Source is mandatory only for work that reads or writes actual chart values.

## Capability 1 — Template family construction and repair

`ENTRY`: current user target, approved family, current source, and expected retained output.

`OPERATION`:

1. Lock whether the job is a new family build, same-family continuation, repair, validation only, or batch expansion.
2. Preserve approved filename grammar, block order, R/A stage role, D1/Target split, index, ending metadata, and prior-layer boundaries.
3. Build or repair only what the current instruction authorizes.
4. Keep raw source, applied structure, QA state, and interpretation layers separate.
5. Run the embedded template engine's applicable lifecycle and validator.

`SECOND GATE`:

- Convert the user's instruction into a complete checkable contract before touching the artifact.
- Treat every required handoff and prohibition as an independent joint.
- Do not call a family complete merely because its central structure looks correct.

`OUTPUT`: a family artifact whose actual members and internal roles match its name and stage.

## Capability 2 — Twelve-house expansion

`JOB UNIT=ONE_D_CHART × ONE_HOUSE × ONE_VIEW`.

For H01 through H12:

1. Re-lock D-chart domain and house function.
2. Read that house's actual Rashi/Bhava and other admitted lane values.
3. Classify `EMPTY / SINGLE / MULTI` from source rather than from a neighboring house.
4. Trace occupant, House Lord, move, handoff, ownership, retention/loss, return, bottleneck, next joint, and residue.
5. Preserve all local missing or conflicting states without deleting confirmed neighbors.
6. Complete the house independently; never create H02–H12 by renaming H01 fields or sentences.

The two user-confirmed split families remain separately available:

- `1H–6H / 7H–12H`.
- `1H–4H / 5H–7H / 8H–12H`.

Do not merge them with the separate `1H–4H / 5H–8H / 9H–12H` candidate unless the user explicitly promotes that revision.

## Capability 3 — Twenty-D-chart generation

The active twenty-chart set is:

`D1,D9,D2,D3,D4,D5,D6,D7,D8,D10,D11,D12,D16,D20,D24,D27,D30,D40,D45,D60`.

- `D50=VOID` for job generation, replacement, and recovery.
- The June 4 PikaChu archive register may present the same set in archive/save order; archive order and active template job order are separate fields.
- Each D-chart retains its own domain and actual body.
- Shared grammar permits a common template, never copied chart values.
- Twenty D-charts × twelve houses equals 240 independent jobs when the user requests the full board.
- Generate and validate a coordinate map once, then hand its ID to downstream jobs; do not rescan the twenty-chart forest inside every house.

## Capability 4 — Work-instruction authoring

A work instruction must make another worker able to execute without reconstructing intent.

Required joints:

1. job character and exclusions;
2. exact input files;
3. prior-layer order;
4. file family and stage role;
5. D1 versus target rule;
6. source-visible and missing-value rules;
7. required internal structure;
8. prohibited imports, overwrites, calculations, and interpretations;
9. independent QA items;
10. exact report format;
11. final filename and retention boundary.

The instruction must distinguish a conceptual dependency from a literal file handoff and name the required preceding artifacts when the stage requires physical inheritance.

## Capability 5 — Structural QA

Before file inspection, decompose the whole work instruction into an exhaustive checklist. Then judge independent axes:

- file count and roster;
- filename and stage role;
- D1/Target separation;
- required sections and subslots;
- Source House/Landing House and similar ownership boundaries;
- missing-value behavior;
- prior-layer overwrite prohibition;
- literal handoff presence;
- title/index/status or selected ending metadata;
- actual completion versus claimed completion.

One failed axis remains local `REVISE` even when every other axis passes. A missing required source or authority becomes local `HOLD`; a direct authority collision becomes `CONFLICT`.

## Capability 6 — Local repair and return test

1. Locate the first effective missing or incorrect joint.
2. Restrict the patch to that joint unless evidence shows a wider dependency.
3. Preserve every already-passing structure.
4. Reopen the actual artifact after repair.
5. Recheck every affected member and the downstream handoff.
6. Recalculate the same judgment to determine `PROGRESS / NO_PROGRESS / LOSS`, bottleneck movement, or renewed leakage.

When the actual Target is bottleneck, repetition, recovery, or path selection, the repair must preserve this exact sequence:

`병목위치 확정 → 병목 원인·손실경로 확정 → 뒤집기 가능한 통제변수 추출 → 뒤집기 관절·조건 확정 → 병목 뒤집기 실행 → 누수 차단 → 동일조건 재투입 → 재누수·재병목 검산 → 대체경로 비교 → 전달·도착·귀속·보유·회수량 재계산`.

Do not merge the condition lock with actual reversal execution, or result occurrence with transfer, arrival, ownership, retention, and recovered amount. Prove `BEFORE/AFTER` numeric recovery only with a verified baseline, unit, and same-condition re-entry; otherwise keep only a structural judgment or a local numeric `HOLD`. For non-bottleneck work this circuit is `NOT_APPLICABLE`.

Do not accept “fixed” as evidence. Physical bytes, member inventory, required text, and ending state must agree.

## Capability 7 — Completion and sign-off

Second-worker completion requires all of the following:

- requested artifact physically exists;
- exact scope is complete;
- no current source was replaced by old grammar values;
- all independent structural judgments pass or preserve an explicit nonblocking local state;
- literal handoffs are present where required;
- repaired files were reopened and checked;
- the next worker can inherit the artifact without returning QA labor to the user.

When the first-worker clean-file gate applies, its formatting and retention check follows structural PASS. A clean-file repair does not erase or replace the second-worker structural judgment.

The outer delivery gate must fail closed unless the packet contains explicit validator PASS results, physical reopen status and evidence, downstream handoff status and evidence, and the independent FNa98 axes. `final_status=PASS` plus empty hold/conflict lists alone is not completion evidence.

## Capability 8 — Blind restoration certification

Use D11 twelve houses as the first bounded blind board. Lock the candidate runtime before revealing an accepted non-3P historical artifact. All `3P` artifacts are user-confirmed DD-second duplicate-reduced compilations of the separate PikaChu files belonging to the same Rashi; they add no distinct calculation, judgment, engine, or new analytic content. Preserve them physically, but keep them operationally `VOID` because their size prevents Google Docs from opening them for the user. Route to the corresponding individual non-3P files, and never treat a `3P` compilation as Source, comparator, closure proof, or quality evidence. This is not a content-quality or authorship failure. If no accepted non-3P comparator is available, keep certification `HOLD`. Include one authority trap and require local `HOLD` or `CONFLICT`. Compare structure, handoff, ownership, bottleneck, next joint, and residue rather than prose. Then run one transfer case on a different input.

Before both tests had passed, only `FUNCTION_RESTORE_SOURCE_GATE=PASS`
could be stated; final functional certification and any
`RESTORED / COMPLETE / FINAL / FNa98` claim were blocked.

The condition above is now satisfied. The embedded records
`KK2_D11_BLIND_CANDIDATE_LOCK.txt`,
`KK2_D10_H10_TRANSFER_TEST.txt`, and
`DCHART_STRUCTURE_02_RESTORE_COMPLETE.txt` record both PASS results and
`FINAL_FUNCTIONAL_CERTIFICATION=PASS`. Treat the pre-pass bullets as a
historical safety gate, not the current continuation state. Do not rerun either
test without an explicit user retest order, and do not promote the result to
canonical state or `FINAL_LOCK` without explicit user approval. Continue with
the current user request under `KK2_WORK_INSTRUCTION.txt`. The authorized scope
may be one `D×H`, one D-chart × 12H, twenty D-charts, an explicit
20D×12H=240 batch, or an exact file family. A single job is the independence
unit, not the capability ceiling.

## Capability 9 — Execute-first work behavior

`CAPABILITY_FIRST=TRUE`. The output can affect the user's life, so exact Target,
admitted Source, complete scope, independent QA, physical reopening, and an
actionable result precede warmth, familiarity, novelty, or worker identity.
Those surface qualities cannot compensate for a capability failure.

When Target, Action, Source, Scope, and Output can be resolved, execute before
describing boot state. Do not repeat a clear question or return a feasibility
plan in place of work. Expand N houses to all twelve and an explicit full-board
request to 240 unique D×H jobs without complaint, neighbor renaming, or a lower
quality floor. Preserve the user's work and every passing joint.

After a correction, name the two mixed layers, preserve the passing structure,
repair the smallest sufficient joint, reopen the physical output, and close the
handoff. Keep the response warm, direct, and result-first. Do not make the user
the final QA worker.

The visible format remains adaptive. Internal gates and handoff fields stay
fixed, but they need not be dumped mechanically into the user-facing answer.
Preserve the PikaChu-set classic core and add a useful user-relevant improvement
only after that core passes. If the first output is rejected, preserve the
passing core internally and reprint a complete revised deliverable. Target user
OK by output two; a needed third output triggers root-cause relocking instead of
another near-identical retry.

## Capability 10 — Final delivery hard gate

An embedded validator's `PASS` text or exit code is insufficient by itself.
Run `scripts/validate_final_delivery.py` on every final packet and require:

- `final_status=PASS`;
- zero holds and conflicts;
- every supplied validator result explicitly `PASS`;
- physical reopening and downstream handoff evidence required by the selected
  route.

Keep `PIKACHU_CANONICAL_PATH` and `DEEP_DENSE_240H_COMMON_PATH` in separate
packets. If both are requested, both must pass independently. Never promote
`PENDING`, `HOLD`, `REVISE`, or `CONFLICT` to final completion.

## Capability 11 — Status-aware archive navigator

When inspecting a PikaChu or source-family archive:

1. Keep the physical member inventory and the active runtime inventory separate.
2. Preserve a `3P` member as DD-second work history while skipping it operationally as `VOID`.
3. Preserve the flat ZIP layout and original filenames; do not silently rename or normalize Source labels.
4. Prefer verified `TITLE`, header, and explicit `STATUS` over a conflicting or copied `INDEX` field.
5. Keep different coverage denominators independent instead of flattening them into one completion percentage.
6. Let the current user instruction, not file adjacency, control the admitted chart, layer, and house scope.

The current attached D1–D5 evidence has 30 physical entries per ZIP: one active index, 28 active content members, and one physically retained but operationally VOID `3P` member. This denominator is proven only for the attached five-ZIP subset and must not be generalized to the uninspected fourteen archives.
